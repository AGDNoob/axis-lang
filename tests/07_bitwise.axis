// AXIS Example 07: Bitwise Operations
// Demonstrates AND, OR, XOR, and shifts
// Expected exit code: 42

func main() -> i32:
    a: i32 = 0b00101010  # 42 in binary
    b: i32 = 0xFF        # 255
    
    # Bitwise AND to extract lower bits
    masked: i32 = a & 0x3F  # Keep lower 6 bits: 42
    
    # Verify with shifts
    shifted: i32 = 21 << 1   # 21 * 2 = 42
    
    when masked == shifted:
        give 42
    
    give 0
