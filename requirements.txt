# AXIS Language - Python Dependencies
# 
# Install with: pip install -r requirements.txt

# Keystone Engine - Multi-architecture assembler framework
# Provides full x86-64 instruction support for compile mode
# Falls back to legacy assembler if not installed
keystone-engine>=0.9.2

# Optional: pyreadline3 for enhanced REPL on Windows
# pyreadline3; sys_platform == 'win32'
