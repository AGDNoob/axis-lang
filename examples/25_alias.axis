mode script

// Basic alias: mutable source → mutable alias
x: i32 = 42
y: i32 = x
writeln(x)
writeln(y)

// Mutation through alias
y = 99
writeln(x)
writeln(y)

// Mutation through source
x = 7
writeln(x)
writeln(y)

// Const alias of const source
const c: i32 = 100
const d: i32 = c
writeln(d)
