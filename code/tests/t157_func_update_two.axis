// EXPECT: 30
mode compile
func swap_add(update a: i32, update b: i32):
    t: i32 = a
    a = b
    b = t
    a = a + b
func main() i32:
    x: i32 = 10
    y: i32 = 20
    swap_add(x, y)
    return x
